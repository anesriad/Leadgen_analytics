# Configs

Configuration and secrets. Currently settings are kept in `.env` (gitignored) and constants inside notebooks/scripts.

- `.env` — GCP project ID, MLflow tracking URI (not committed)
- `service_account.json` — GCP credentials placeholder (gitignored)
